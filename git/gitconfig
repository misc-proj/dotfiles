[hub]
	protocol = https
[user]
	name = Joshua Rubin
	email = jrubin@zvelo.com
[alias]
	sts = status
	st = status
	ci = commit -s
	am = am -s
	di = diff
	dis = diff
	co = checkout
	br = branch
	lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
[color]
	diff = auto
	status = auto
	branch = auto
	ui = true
	grep = auto
[core]
	excludesfile = ~/.gitignore
	editor = nvim
[apply]
  whitespace = nowarn
[mergetool]
	keepBackup = false
[difftool]
	prompt = false
[help]
	autocorrect = 1
[push]
	default = simple
[include]
	path = .gitconfig.user
[filter "lfs"]
	clean = git-lfs clean %f
	smudge = git-lfs smudge %f
	required = true
